enum LocalPaymentMethods {
  telebirr,
  mpessa,
  cbebirr,
  ebirr,
}

enum RequestType {
  get,
  post,
  patch,
  put,
  delete,
}

enum PaymentStatus {
  pending,
  success,
}

enum Mode {
  live,
  testing,
}

enum VerificationType {
  ussd,
  otp,
}
